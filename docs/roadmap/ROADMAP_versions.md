# CSSA-DA 版本路线图

本文定义**每个版本的边界**:开放给谁、承诺什么、三条线各做什么、什么时候算做完。

三条线的详细内容见:

| 线 | 文档 |
|---|---|
| 平台与部署 | [ROADMAP_platform.md](ROADMAP_platform.md) |
| 数据与语料 | [ROADMAP_data.md](ROADMAP_data.md) |
| 查询链路与评估 | [ROADMAP_rag.md](ROADMAP_rag.md) |

**本文只回答:什么时候能给谁用、他能得到什么。**

---

## 目录

- [里程碑 ≠ 版本号](#里程碑--版本号)
- [划分原则](#划分原则)
- [v1「能跑」—— 内测](#v1能跑--内测)
- [v2「能用」—— 知识库真的有内容](#v2能用--知识库真的有内容)
- [v3「能信」—— 有尺子,选型完成](#v3能信--有尺子选型完成)
- [v4「能活」—— 运营化](#v4能活--运营化)
- [多源不是预案,是主线](#多源不是预案是主线2026-08-11-改口径)
- [明确不在任何版本里](#明确不在任何版本里)
- [条目 → 版本映射](#条目--版本映射)

---

## 里程碑 ≠ 版本号

**本文写的 v1–v4 是产品里程碑,不是发布版本号。** 两者是**多对一**:

```
v1「能跑」这个里程碑
   ├── v0.1.0  首次部署上线
   ├── v0.1.1  修了拒答的 bug
   ├── v0.1.2  加了 chat_interactions 的一列
   └── v0.2.0  换了 reranker
        ↓ 都还在 v1 里程碑内
```

| | 回答什么 | 在哪里 |
|---|---|---|
| **里程碑**(本文) | 什么时候能给谁用 | 本文 |
| **版本号** | 线上这一刻跑的是哪一份东西 | git tag |

发布流程、commit 规范、四种版本坐标见
[CONTRIBUTING.md](../../CONTRIBUTING.md#5-版本与发布)。

### 四种版本坐标

出事时要能在一分钟内回答「这个坏回答是哪份代码 + 哪个镜像 + 哪份语料 + 哪个模型
产生的」:

| 坐标 | 形式 | 现状 |
|---|---|---|
| 代码版本 | git tag | ❌ **仓库里零个 tag** |
| 制品版本 | 镜像 tag = git sha(immutable) | 📋 已计划 |
| API 契约版本 | `/v1/chat` | ❌ 没有 —— **v1 窗口期** |
| 数据版本 | 语料 `sha256` | ✅ 已设计 |

第四种你们**已经做对了**(`corpus_sha256`、`data/eval/v1/` 递增保留、报告必须带
hash)。那套纪律搬到代码上就是标准的版本管理。

线上每条 `chat_interactions` 记录都要带齐这四个坐标 —— 见
[ROADMAP_rag.md](ROADMAP_rag.md) Phase 4.5 的 `config` 指纹。

---

## 划分原则

**按能力承诺划,不按技术里程碑划。** 版本要能回答「什么时候能给谁用、承诺他能得到
什么」,而不是「哪个模块做完了」。

一句话概括四个版本:**能跑 → 能用 → 能信 → 能活**。

| 版本 | 一句话 | 卡在什么上 |
|---|---|---|
| **v1 能跑** | 链路验证 + 开始攒真实 query | 平台线 Phase 2(长杆)+ 前端架构决策 |
| **v2 能用** | 知识库第一次真的有内容 | **能拿到多少个源**(小助手是其中之一,不是全部) |
| **v3 能信** | 质量有据可依,而不是感觉 | 标注人力(4–5 人天)+ v2 的数据 |
| **v4 能活** | 系统能自己变好 | 前面都做完 + 有人持续运营 |

**v2 最不可控的部分是外部依赖。** 小助手导出探索的周期不由我们控制,所以虽然不阻塞
v1,**应该现在就派人去做**。但它已经不是 v2 的唯一入口 —— 见
[多源不是预案,是主线](#多源不是预案是主线2026-08-11-改口径)。

---

## v1「能跑」—— 内测

| | |
|---|---|
| **开放给** | 社团内部 + 少量志愿者(几十人) |
| **承诺** | 能答课程类问题(handbook);**答不了的时候明确说资料不足** |
| **不承诺** | 覆盖面、准确率 |
| **核心产出** | ① 端到端链路验证过 ② **开始积累真实 query** |

### Platform

- [ ] Phase 2 全部:IaC(VPC/SG/ECR/ECS/ALB/RDS/Secrets/CloudWatch/IAM)、部署、smoke test
- [ ] 第 8 项 ECS/ALB health check 配置
- [ ] 第 11 项 migration 部署关卡
- [ ] 第 12 项 outbound networking
- [ ] 第 17 项 生产 RDS 配置
- [ ] 第 14 项 资源基线(粗版,够填 task 规格即可)
- [x] **第 19 项 前端接入架构决策** —— **已定(2026-09-06)**:v1 不走 BFF、浏览器直连,
      key 由 Django 服务端注入模板不进 commit;临时 token 推到 v2,触发器是 streaming。
      见 [ROADMAP_platform 19.8](ROADMAP_platform.md)
- [x] 19.4 三道成本封顶 —— **三道齐了(2026-09-06)**:OpenAI 支出上限 ✅ /
      全局限流 ✅ / `/chat` 输入体积 ✅。19.8「v1 直连」的前提于此成立
- [ ] **key 不进 commit** —— 交代给前端团队的一句话(仓库是 public,「不公开」这条路不存在)
- [ ] **鉴权形状重构**(`Principal`)—— v1 唯一的代码活,纯重构、行为零变化,见 19.9
- [ ] **CORS 认真配** —— 直连之后它从摆设变成承重墙;部署那天配生产域名 + smoke test
      加正反两条断言

### Data

- [ ] 0.1 handbook 重跑抓取(现有数据只覆盖到字母序的 ERTH)
- [x] 0.3 微信文章定位 —— **已决定(2026-08-11):v1 原样保留,不筛选**。定位问题
      推到 v2,理由和已知风险见 [ROADMAP_data 0.3](ROADMAP_data.md#03-微信文章的定位--v1-已拍板原样保留)
- [ ] 0.2 小助手导出探索(**启动即可,不要求完成**)

**v1 的知识库 = handbook + 微信文章**,就这两样。

### RAG

- [ ] 0.1 doc_id 链路 ⏰ **唯一有硬截止的契约变更**
- [ ] `/chat` → `/v1/chat`(与 doc_id 同一窗口 —— 现在没有消费者,成本最低)
- [x] 0.3 `top_k` 调大 —— **已落地(2026-08-25):retriever 5 → 30、reranker 3 → 5**。终值仍待 Phase 5.6 的 Recall@k 曲线
- [ ] 0.4 换 reranker(✅ 已定)
- [x] 0.6 first-request 冷惩罚
- [ ] 0.8 `/chat` 输入体积上限(两行)
- [ ] 3.1 拒答测试
- [ ] 4.1 检索结果落结构化日志
- [x] 4.5 `chat_interactions` 六列 + `BackgroundTasks` 写入

### 退出标准

1. 新生用中文提问,能得到基于 handbook 的正确课程回答,**答不了时明确说资料不足**
2. 匿名/无效客户端调不动 `/chat`;超频返回 429
3. 每次请求有结构化日志(request_id、query、检索到的 doc_id、各段耗时)
4. 每次请求在 `chat_interactions` 落一行,**带 config 指纹**
5. 新 task 通过 `/ready` 后,**检索 + 重排**的首请求与稳态延迟差 < 200ms
   (生成段不计入 —— 剩余差值由首次 OpenAI 连接建立主导,不是我们能优化的部分,
   单独观测、不设阈值。见 [ROADMAP_rag 0.6](ROADMAP_rag.md#06-first-request-冷惩罚))
6. 滚动发布不丢在途请求;migration 作为部署关卡执行
7. 能从日志 + `chat_interactions` 复盘任意一次坏回答
8. **打出第一个 git tag `v0.1.0`**;镜像 tag = git sha(immutable,禁止用 `latest` 部署)

### 风险

**知识库只有 handbook,能答的问题很窄。** 所以必须是内测,**不能对外宣传成
「留学助手」** —— 期望管理比技术更重要。

### 长杆

Platform Phase 2。RAG 那几项加起来不到一周,**塞进 IaC 的等待窗口**。

---

## v2「能用」—— 知识库真的有内容

**这是产品能力的质变点** —— 知识库第一次真的包含用户想问的东西。

| | |
|---|---|
| **开放给** | 扩大内测(几百人) |
| **承诺** | 能答留学生活常见问题(语言班、注册、租房、开卡…) |
| **核心产出** | 知识库有内容了;**eval query 的来源也有了** |

> **口径已改(2026-08-11)**:本版原名「小助手数据进库」。现在的定义是**覆盖到新生
> 真会问的领域**,来源不限于小助手 —— 见
> [多源不是预案,是主线](#多源不是预案是主线2026-08-11-改口径)。

### Data(本版主体)

- [ ] Phase 1.1 聊天记录 → 问答对(规则粗切 + LLM 提取 + 人工抽检 50 场)
- [ ] Phase 1.2 PII 脱敏 + 个人化内容筛选
- [ ] Phase 1.3 多源 loader + 统一 schema(含 `modality` 字段)
- [ ] Phase 1.4 聚簇(产出「≥2 条问答簇」的数量 —— 这是 v3 gold set 规模的上限)
- [ ] Phase 1.5 冻结 + `manifest.json`
- [ ] **新源接入** —— 候选见下方「v2 的候选源」,清单确定后再细化成 issue
- [ ] **权威优先(时效处置 Layer 1)** —— 源上加 tier + system prompt 规定「事实性
      结论以权威源为准」。**必须和新源接入同一版做**:光加官方源不会让旧内容自动输,
      检索按相似度排、不按权威度。见
      [ROADMAP_data 时效问题的处置](ROADMAP_data.md#时效问题的处置2026-08-11-定)
- [ ] **0.3 微信文章定位**(从 v1 推迟过来)—— 有了别的源做对照才判断得了

### v2 的候选源

按**「能不能稳定合法地拿到」**排序,不按内容多少排 —— 这才是真正的筛子:

| 源 | 模态 | 拿得到吗 | 备注 |
|---|---|---|---|
| CSSA 自己整理的 FAQ / 新生手册 | `qa` | ✅ 最省事 | **先问一句有没有现成的**,成本最低 |
| 墨大官网(Stop 1、注册、选课、census date、special consideration) | `doc` | ✅ 公开网页 | 复用 handbook 修好的抓取脚本 |
| Trinity 语言班页面 | `doc` | ✅ | 高频问题,handbook 里没有 |
| Home Affairs 签证页(500 签、8105 工时、续签) | `doc` | ✅ 公开 | ⚠️ **变更频繁,过期即错**,且后果比社团推文重得多 |
| Services Australia / ATO(Medicare、TFN、报税) | `doc` | ✅ | 同上 |
| PTV concession 资格 | `doc` | ✅ | 留学生高频,规则常年有争议 |
| 小红书 | `qa` + `doc` **混合** | ⚠️ 见下 | 内容相关性最高,获取难度也最高 |
| 论坛帖子 | `qa` + `doc` **混合** | ⚠️ 取决于站点 | 同样是混合模态 |
| 群聊 | `qa` | ❌ 隐私路径未定 | 已在 Phase 4 |

#### 关于小红书

**内容上它可能是相关性最高的源** —— 留学生活的真实经验(租房、开卡、看病、
选课踩坑)正是 handbook 和官网答不了、而新生最想问的那一类。

但它有三层别的源没有的阻力,**排期时要按最坏情况估**:

1. **没有公开 API,且有较强的反爬** —— 这不是「写个脚本抓一下」的量级
2. **ToS 与合规** —— 批量抓取第三方平台的用户内容,和抓学校官网不是一回事,
   需要先想清楚立场
3. **可信度低且参差** —— 民间经验帖里错误信息很常见(签证、税务尤其)。它落在
   [四根正交的轴](ROADMAP_data.md#四根正交的轴)里 `authority` = 民间那一档,
   **这也是 `authority` 这根轴第一次真的需要被实现的时点**

**它还是 `modality` 混合源的第一个真实案例** —— 同一个源里,提问帖是 `qa`、
长经验帖是 `doc`。这正是[「不要从 `source` 推导 `modality`」](ROADMAP_rag.md#222-源会越来越多索引不能跟着源走)
那条约束防的情况。

> **建议的顺序**:先做 CSSA FAQ 和墨大官网(便宜、无阻力、能立刻验证多源链路
> 通不通),把小红书放在它们之后 —— 而不是反过来。否则很可能在最难的源上卡住,
> 而整条多源链路一次都没跑通过。

### Platform

- [ ] Phase 3 S3Storage(开始定期跑管线了)
- [ ] **PII 脱敏作为 pipeline stage**,在入库之前执行
- [ ] **临时 token 接入**(从 v1 推迟过来)—— 浏览器不再持长期 key,身份第一次可信。
      **主触发器是本版的 streaming**(RAG 0.7),排同一个窗口;五步切换与那条最容易忘的
      「轮换 `CHAT_API_KEY`」见 [ROADMAP_platform 19.8](ROADMAP_platform.md)
- [ ] **第 16 项 按用户限流** —— 依赖上一条,身份可信之后才有意义

### RAG

无必须项。建议补 Phase 4 剩余可观测性、Phase 1/2(见下方提前建议)。

- [ ] `chat_interactions` 加 `user_id` 列(可空)—— 与临时 token 同批,**v1 期间的行是
      NULL**。在 v1 就加等于填一列客户端可伪造的值

### 退出标准

1. **至少两个新源入库,且其中至少一个是文档类** —— 原文写的是「小助手 QA 对入库」,
   在多源口径下改成这条:它不押注于任何单一外部依赖
2. 抽检 100 条,**零 PII 残留**
3. 真实问题的可答率相比 v1 显著上升(用 v1 攒的 query 做对照)
4. **分源指标各自成立** —— 不能出现「总体上去了但某一类源系统性检索不到」。混合
   语料下这是模态偏置唯一的早期信号,见
   [ROADMAP_rag](ROADMAP_rag.md#12-指标与健康度) Phase 1.2

### 这一版会兑现一个 v1 埋下的决定

小助手数据进来后,**2312 篇微信推广文的噪声会凸显** —— v1 里 0.3 那个 (a)/(b)/(c)
的选择在这里见分晓。若选了 (b)「清出知识库」,执行也在这一版。

---

## v3「能信」—— 有尺子,选型完成

| | |
|---|---|
| **开放给** | **全体新生** |
| **承诺** | 同 v2,但质量**有据可依**而不是感觉 |
| **核心产出** | 一把尺子 + 一个经过验证的配置 |

### Data

- [ ] Phase 2 gold set(标注 guideline + kappa、query 收集、pooling、标注、打包)
- [ ] Phase 3 架构与模型选型
- [ ] Phase 5 落 schema(可能含向量维度迁移)

### RAG

- [ ] Phase 1 评估工具
- [ ] Phase 2 可插拔的检索方案
- [ ] Phase 5 跑实验矩阵(baseline → doc2query pilot → 2×2 → C 条件分支 → reranker)

### Platform

- [ ] 若换大模型:镜像 / ECS 规格重测(Phase 1 的验收基线全部作废)
- [ ] 第 13 项 worker 与扩容策略

### 退出标准

胜出配置相对 baseline 的提升**经过 bootstrap 检验显著**,judged fraction ≥ 80%,
且线上抽样结果与离线 eval 一致。

### ⚠️ 一个提前建议

**RAG 线的 Phase 1(评估工具)和 Phase 2(可插拔架构)应该提前到 v1–v2 期间做。**

它们**不依赖 ground truth**,而 Phase 2 直接决定 v3 的实验是「跑四次」还是
「重写三遍」。放到 v3 才做,等于把 v3 的成本翻几倍。

### 这一版的性质

**唯一人力密集的版本**(标注 4–5 人天),也是**唯一可能触发向量维度迁移**的版本。

---

## v4「能活」—— 运营化

| | |
|---|---|
| **开放给** | 公开 + 持续运营 |
| **核心产出** | 系统能自己变好,而不是靠人推 |

### Platform

- [ ] Phase 4 CI/CD(GitHub Actions + IAM OIDC、immutable tag、部署前 migration、回滚)
- [ ] 告警、dashboard、capacity metric
- [ ] 第 16 项 精细化限流(若 v1 未做)
- [ ] 第 15 项 业务级可观测性字段

### RAG

- [ ] `POST /feedback` + 反馈列(前端加按钮)
- [ ] Phase 3 其余生成器契约(引用格式、截断、注入防护)
- [ ] Phase 6 持续调优、reranker PEFT/LoRA

### Data

- [ ] Phase 4 群聊接入(v2 数据集)—— **隐私路径必须先定,周期长,建议提前启动**
- [ ] Phase 6 飞轮 + `authored_blind` vs `human_log` 交叉验证

### 飞轮在这一版闭合

```
答不上来 → 标记缺口 → 小助手回答 → 进知识库 → 下次能答
```

---

## 多源不是预案,是主线(2026-08-11 改口径)

> ⚠️ **本节原标题是「分支预案:小助手数据导不出来」**,把「其他数据源」写成导不出来
> 时才走的备胎。**这个口径已经作废** —— 已经确定**只靠小助手问答覆盖不了新生真正
> 会问的东西**,学校官网、政府签证页这类文档源是必需品,不是替代品。

```
原口径:  v1 ──► v2  小助手进库 ──► v3 选型 ──► v4 运营
         （导不出来才走）v2' 其他数据源

新口径:  v1 ──► v2  知识库覆盖到新生真会问的领域 ──► v3 选型 ──► v4 运营
                    └─ 小助手问答是其中一个来源，不是全部
```

**v2 的定义随之改变**:从「小助手数据进库」改成「**知识库第一次真的覆盖新生会问的
领域**」。小助手能不能导出仍然重要,但它现在只决定 v2 的**成色**,不再决定 v2 的
**有无**。

由此产生的三个后果:

1. **语料永久异构。** 问答类 + 文档类会长期并存 —— 这是
   [ROADMAP_data](ROADMAP_data.md#一个必须先说清楚的前提) 开篇那条前提改写的原因,
   也让 [ROADMAP_rag](ROADMAP_rag.md#phase-2可插拔的检索方案) Phase 2 从「三选一的
   候选」变成「结构性决定」。
2. **小助手导出调研的分量下降。** 它原先被记为「blocks 整条数据线」,现在只 blocks
   自己那一块。数据线可以在它有结论之前就开始推进别的源。
3. **时效性成为一等问题。** 政府签证页、学校注册截止日期这类内容**变更频繁,过期
   即错**,而且后果比社团推文重得多 —— 签证信息答错不是体验问题。每个源都必须带
   `post_date` 和一个复查周期。

### 若小助手确实导不出来

- gold set 退回**闭卷编写 150 条**,基于 handbook + 已有语料。仍然能做选型,
  **成色打折但不是死路**
- **真实 query 从哪来?** —— **v1 的 `chat_interactions` 就是备份来源**

> 原来的预案意味着两条纪律,在新口径下**更该守**:
> **① v1 不要依赖小助手数据 ② v1 的交互记录必须早做**

---

## 明确不在任何版本里

写下来免得反复讨论:

- **自托管 LLM** —— 生成走 OpenAI,除非账单失控
- **多实例水平扩容 / Redis 分布式限流** —— 社团规模用不上,单实例足够
- **GPU 推理** —— 除非 CPU 实测数据证明必要
- **LangSmith 生产 tracing** —— 开发环境用,生产走自己的结构化日志
- **Prompt 脱离 git 的云端管理** —— 生产行为要留在版本控制里

---

## 条目 → 版本映射

### Platform

| 条目 | 版本 |
|---|---|
| 1–7、9、10 | ✅ 已完成 |
| 8 health check / 11 migration 关卡 / 12 outbound / 17 RDS | **v1** |
| 14 资源基线 / 19 前端安全边界 | **v1** |
| 16 精细化限流 | **v2** —— 与临时 token 同批(v1 直连,per-IP 仍有区分度) |
| Phase 3 S3 pipeline | **v2** |
| 13 扩容策略 | **v3** |
| Phase 4 CI/CD、15 业务可观测性 | **v4** |
| Phase 4.2 代码质量工具链(ruff + mypy) | **v2 建议提前** —— 趁 RAG 线大批新代码之前立规则 |

### Data

| 条目 | 版本 |
|---|---|
| 0.1 handbook 重抓 / 0.3 微信定位 | **v1** |
| 0.2 小助手导出探索 | **v1 启动**,**v2 完成** |
| Phase 1 语料建设 | **v2** |
| Phase 2 gold set / Phase 3 选型 / Phase 5 落 schema | **v3** |
| 0.4 群聊隐私路径 | **v3 启动**(周期长),**v4 完成** |
| Phase 4 群聊接入 / Phase 6 运营 | **v4** |

### RAG

| 条目 | 版本 |
|---|---|
| 0.1 doc_id / 0.3 top_k / 0.4 reranker / 0.6 冷惩罚 / 0.8 输入上限 | **v1** |
| 3.1 拒答测试 / 4.1 检索落日志 / 4.5 交互记录 | **v1** |
| 0.2 合并 evaluator / 0.7 stream 双路径 | **v2**(建议) |
| **Phase 1 评估工具 / Phase 2 可插拔架构** | **v2 提前做**(见 v3 的提前建议) |
| Phase 5 跑实验 | **v3** |
| 0.5 难负例训练 / Phase 3 其余 / Phase 6 | **v4** |
